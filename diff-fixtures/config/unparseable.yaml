# Broken on purpose, so the review page has something to say "not valid YAML"
# about — and can say it with a line number rather than a shrug.
retries: 3
backoff:
  initialMs: 250
  maxMs: [8000
targets:
  - name: primary
    url: https://example.invalid/a
  - name: secondary
    url: https://example.invalid/b
