# Valid on this side. The pull request breaks it on purpose, so the review
# page has something to say "not valid YAML, at line N" about.
retries: 3
backoff:
  initialMs: 250
  maxMs: 8000
targets:
  - name: primary
    url: https://example.invalid/a
  - name: secondary
    url: https://example.invalid/b
