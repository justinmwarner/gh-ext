node_modules/
dist/
.output/
.wxt/
*.log

# Playwright
test-results/
playwright-report/

# The store walkthrough. Regenerated by `npm run tour`, a megabyte a time, and
# it changes wholesale on every run — the listing takes a YouTube URL rather
# than the file, so nothing downstream needs the bytes to be in git.
store/video/

# Never commit a packing key
*.pem
.extension-key*

# Local Claude Code settings. Machine-specific, and not something a public
# repository should carry.
.claude/settings.local.json
